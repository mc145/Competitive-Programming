if(b[b.size() - 1] % ins[i] == 0){
            //     if(b[b.size() - 1]/ins[i] > maxs){
            //         pos = i; 
            //         maxs = b[b.size() - 1]/ins[i]; 
            //     }
            //  }