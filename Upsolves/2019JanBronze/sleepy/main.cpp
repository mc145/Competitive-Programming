#include<iostream>
#include<algorithm>
#include<vector>
#include<math.h>
#include<string> 
#include<fstream>

using namespace std; 


int main(){


int n; 
cin >> n; 

vector<int> ins; 
for(int i = 0; i<n; i++){
    int a; 
    cin >> a; 
    ins.push_back(a); 
}

// the last n - k cows will have to remain unchanged, if k is the answer => so dumb to not notice that


}